apiVersion: v1
kind: Service
metadata:
  name: simple-html-service
spec:
  selector:
    app: simple-html
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort   # Local cluster এ NodePort ব্যবহার করো

